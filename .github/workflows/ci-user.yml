name: CI user-service

on:
  push:
    branches: [main]
    paths:
      - 'FlipNote-User/**'
      - '.github/workflows/ci-user.yml'
  pull_request:
    branches: [main]
    paths:
      - 'FlipNote-User/**'
      - '.github/workflows/ci-user.yml'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./FlipNote-User
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: temurin
      - uses: gradle/actions/setup-gradle@v4
      - run: chmod +x gradlew
      - run: ./gradlew build -x test
      - run: ./gradlew test
