apiVersion: v1
kind: Service
metadata:
  name: {{ include "image-service.fullname" . }}
  labels:
    {{- include "image-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "image-service.selectorLabels" . | nindent 4 }}
